diff --git a/question_generation/config.py b/question_generation/config.py
index 3810f4a..4e2661a 100644
--- a/question_generation/config.py
+++ b/question_generation/config.py
@@ -9,7 +9,6 @@ DEFAULT_DISTRACTOR_COUNT = 3
 DEFAULT_RANDOM_SEED = 7
 DEFAULT_SUPPORTED_QUESTION_TYPES = (
     "claim_choice",
-    "boolean_support",
     "two_hop_tail",
     "essay",
     "experiment_code",
diff --git a/question_generation/dedup.py b/question_generation/dedup.py
index 7d383a3..21cfc87 100644
--- a/question_generation/dedup.py
+++ b/question_generation/dedup.py
@@ -3,9 +3,12 @@ from __future__ import annotations
 import json
 import logging
 from pathlib import Path
+from typing import Any
 
 from pubmed_graph.utils import normalize_text
 
+from .indexing import canonicalize_entity
+
 
 logger = logging.getLogger("question_generation.dedup")
 
@@ -14,11 +17,63 @@ def normalize_question_text(text: str) -> str:
     return normalize_text(text).casefold()
 
 
+def _field(obj: Any, name: str, default: Any = None) -> Any:
+    if isinstance(obj, dict):
+        return obj.get(name, default)
+    return getattr(obj, name, default)
+
+
+def _answer_text(row: dict[str, Any]) -> str:
+    answer = row.get("answer", "")
+    return str(_field(answer, "canonical_text", "") or _field(answer, "text", "") or answer or "")
+
+
+def _option_texts(row: dict[str, Any]) -> tuple[str, ...]:
+    options = row.get("options", []) or []
+    out: list[str] = []
+    if not isinstance(options, list):
+        return ()
+    for option in options:
+        text = _field(option, "text", "")
+        if text:
+            out.append(str(text))
+    return tuple(out)
+
+
+def _canonical_option_set(row: dict[str, Any]) -> tuple[str, ...]:
+    return tuple(sorted(canonicalize_entity(text) for text in _option_texts(row) if canonicalize_entity(text)))
+
+
+def dedup_key(row: dict[str, Any]) -> str:
+    """Return a collision-resistant key for algorithmically distinct samples.
+
+    Multiple-choice questions, especially claim_choice, can share the same
+    stem while having different option sets and different correct answers. For
+    those rows the key must include options and answer; for open-ended rows the
+    normalized question text remains the conservative de-duplication key.
+    """
+    question = normalize_question_text(str(row.get("question", "")))
+    qtype = str(row.get("question_type", "")).casefold()
+    if qtype in {"claim_choice", "one_hop_tail", "two_hop_tail", "boolean_support"}:
+        answer = canonicalize_entity(_answer_text(row))
+        options = _canonical_option_set(row)
+        return json.dumps(
+            {
+                "question_type": qtype,
+                "question": question,
+                "answer": answer,
+                "options": options,
+            },
+            sort_keys=True,
+        )
+    return question
+
+
 def deduplicate_by_question(rows: list[dict]) -> list[dict]:
     seen: set[str] = set()
     kept: list[dict] = []
     for row in rows:
-        key = normalize_question_text(str(row.get("question", "")))
+        key = dedup_key(row)
         if not key or key in seen:
             continue
         seen.add(key)
@@ -27,12 +82,11 @@ def deduplicate_by_question(rows: list[dict]) -> list[dict]:
 
 
 def load_seen_questions(paths: list[str | Path]) -> set[str]:
-    """Read one or more samples.jsonl files and return the set of
-    normalized question texts already produced by those runs.
+    """Read one or more samples.jsonl files and return previously seen keys.
 
-    Used by ``--dedup-against`` in the CLI to skip questions that
-    already exist in a previous run's output, so two graphs don't
-    re-emit the same questions when their triple sets overlap.
+    Used by ``--dedup-against`` in the CLI to skip samples that already exist
+    in previous outputs. Multiple-choice keys include options and answers so
+    distinct candidate-claim questions with the same stem are not collapsed.
     """
     seen: set[str] = set()
     for raw_path in paths:
@@ -50,25 +104,25 @@ def load_seen_questions(paths: list[str | Path]) -> set[str]:
                     row = json.loads(line)
                 except json.JSONDecodeError:
                     continue
-                key = normalize_question_text(str(row.get("question", "")))
+                key = dedup_key(row)
                 if key:
                     seen.add(key)
         logger.info(
-            "--dedup-against %s: loaded %d new question keys (set size: %d)",
+            "--dedup-against %s: loaded %d new sample keys (set size: %d)",
             p, len(seen) - count_before, len(seen),
         )
     return seen
 
 
 def deduplicate_against(rows: list[dict], seen: set[str]) -> tuple[list[dict], int]:
-    """Drop rows whose normalized question text is already in ``seen``.
+    """Drop rows whose de-duplication key is already in ``seen``.
 
     Returns ``(kept, dropped_count)``.
     """
     kept: list[dict] = []
     dropped = 0
     for row in rows:
-        key = normalize_question_text(str(row.get("question", "")))
+        key = dedup_key(row)
         if key and key in seen:
             dropped += 1
             continue
diff --git a/question_generation/evidence_claims.py b/question_generation/evidence_claims.py
index 1b60b44..1fb5cb0 100644
--- a/question_generation/evidence_claims.py
+++ b/question_generation/evidence_claims.py
@@ -13,9 +13,58 @@ def _evidence_phrase(subgraph) -> str:
     return normalize_text(subgraph.supporting_triples[0].evidence)
 
 
+def _relation_text(relation: str) -> str:
+    return normalize_text(relation).replace("_", " ")
+
+
+def _build_two_hop_claim_texts(subgraph, evidence_profile: dict[str, object]) -> dict[str, str]:
+    first, second = subgraph.edges[:2]
+    title = _first_chunk_title(subgraph)
+    evidence = _evidence_phrase(subgraph)
+    rel1 = _relation_text(first.relation)
+    rel2 = _relation_text(second.relation)
+    intermediate = first.tail
+    evidence_strength = str(evidence_profile.get("evidence_strength", "medium"))
+
+    if evidence_strength == "weak":
+        claim_text = (
+            "The evidence weakly suggests a possible two-hop chain in which "
+            f"{first.head} {rel1} {intermediate}, and {intermediate} {rel2} {second.tail}."
+        )
+        conservative = claim_text
+    else:
+        claim_text = (
+            "The available evidence supports a two-hop chain: "
+            f"{first.head} {rel1} {intermediate}, and {intermediate} {rel2} {second.tail}. "
+            f"The supported intermediate entity is {intermediate}."
+        )
+        conservative = (
+            "The reported evidence indicates the complete chain "
+            f"{first.head} {rel1} {intermediate} and {intermediate} {rel2} {second.tail}; "
+            f"{intermediate} is the unique supported intermediate entity."
+        )
+    if evidence:
+        claim_text = f"{claim_text} Evidence snippet: {evidence}"
+    query_text = normalize_text(f"{first.head} {intermediate} {second.tail} {rel1} {rel2} {title}")
+    chain_claim = (
+        "The evidence supports a complete two-hop chain: "
+        f"{first.head} {rel1} {intermediate}, and {intermediate} {rel2} {second.tail}. "
+        f"{intermediate} is the unique supported intermediate entity."
+    )
+    return {
+        "claim_text": claim_text,
+        "claim_text_conservative": conservative,
+        "query_text": query_text,
+        "chain_claim_text": chain_claim,
+    }
+
+
 def build_claim_texts(subgraph, evidence_profile: dict[str, object]) -> dict[str, str]:
+    if len(getattr(subgraph, "edges", []) or []) >= 2 or subgraph.question_type == "two_hop_tail":
+        return _build_two_hop_claim_texts(subgraph, evidence_profile)
+
     edge = subgraph.edges[0]
-    relation = edge.relation.replace("_", " ")
+    relation = _relation_text(edge.relation)
     title = _first_chunk_title(subgraph)
     evidence = _evidence_phrase(subgraph)
     evidence_strength = str(evidence_profile.get("evidence_strength", "medium"))
diff --git a/question_generation/evidence_profiler.py b/question_generation/evidence_profiler.py
index a5eea28..e64dab6 100644
--- a/question_generation/evidence_profiler.py
+++ b/question_generation/evidence_profiler.py
@@ -4,6 +4,9 @@ from functools import lru_cache
 
 from pubmed_graph.utils import normalize_text
 
+from .evidence_utils import independent_doc_count, independent_support_count
+from .indexing import canonicalize_entity
+
 HEDGE_MARKERS = (
     "suggest",
     "suggested",
@@ -39,6 +42,8 @@ WEAK_RELATIONS = {
     "part_of",
 }
 
+_STRENGTH_RANK = {"weak": 0, "medium": 1, "strong": 2}
+
 
 @lru_cache(maxsize=1)
 def _ontology_strength_map() -> dict[str, str]:
@@ -66,8 +71,12 @@ def _ontology_strength_map() -> dict[str, str]:
     return out
 
 
+def _relation_key(relation: str) -> str:
+    return normalize_text(relation).replace(" ", "_").casefold()
+
+
 def _relation_strength(relation: str) -> str:
-    normalized = normalize_text(relation).replace(" ", "_").casefold()
+    normalized = _relation_key(relation)
     # 1. ontology-declared strength wins (lets the OntologyProposerAgent
     #    flag a new mechanistic verb as strong without touching this file)
     onto_map = _ontology_strength_map()
@@ -89,27 +98,96 @@ def _hedge_score(texts: list[str]) -> float:
     return min(hits / 3.0, 1.0)
 
 
+def _triples_for_edge(subgraph, edge) -> list:
+    head = canonicalize_entity(edge.head)
+    relation = _relation_key(edge.relation)
+    tail = canonicalize_entity(edge.tail)
+    out = []
+    for triple in subgraph.supporting_triples:
+        if canonicalize_entity(triple.head) != head:
+            continue
+        if _relation_key(triple.relation) != relation:
+            continue
+        if canonicalize_entity(triple.tail) != tail:
+            continue
+        out.append(triple)
+    return out
+
+
+def _weakest_strength(strengths: list[str]) -> str:
+    if not strengths:
+        return "medium"
+    return min(strengths, key=lambda value: _STRENGTH_RANK.get(value, 1))
+
+
 def profile_subgraph_evidence(subgraph) -> dict[str, object]:
-    evidence_texts = [triple.evidence for triple in subgraph.supporting_triples if normalize_text(triple.evidence)]
+    edges = list(getattr(subgraph, "edges", []) or [])
+    hop_profiles: list[dict[str, object]] = []
+
+    if edges:
+        for edge in edges:
+            triples = _triples_for_edge(subgraph, edge)
+            evidence_texts = [triple.evidence for triple in triples if normalize_text(triple.evidence)]
+            strength = _relation_strength(edge.relation)
+            hop_profiles.append(
+                {
+                    "head": edge.head,
+                    "relation": edge.relation,
+                    "tail": edge.tail,
+                    "relation_strength": strength,
+                    "hedge_score": _hedge_score(evidence_texts),
+                    "support_count": independent_support_count(triples),
+                    "doc_count": independent_doc_count(triples),
+                }
+            )
+    else:
+        relation = subgraph.prompt_relation.split(" -> ")[0] if " -> " in subgraph.prompt_relation else subgraph.prompt_relation
+        evidence_texts = [triple.evidence for triple in subgraph.supporting_triples if normalize_text(triple.evidence)]
+        hop_profiles.append(
+            {
+                "head": getattr(subgraph, "prompt_subject", ""),
+                "relation": relation,
+                "tail": getattr(subgraph, "target_answer", ""),
+                "relation_strength": _relation_strength(relation),
+                "hedge_score": _hedge_score(evidence_texts),
+                "support_count": independent_support_count(subgraph.supporting_triples),
+                "doc_count": independent_doc_count(subgraph.supporting_triples),
+            }
+        )
+
     chunk_texts = [chunk.text for chunk in subgraph.supporting_chunks if normalize_text(chunk.text)]
-    relation = subgraph.prompt_relation.split(" -> ")[0] if " -> " in subgraph.prompt_relation else subgraph.prompt_relation
-    relation_strength = _relation_strength(relation)
-    hedge_score = _hedge_score([*evidence_texts, *chunk_texts[:1]])
-    support_count = len(subgraph.supporting_triples)
-    doc_count = len({triple.doc_id for triple in subgraph.supporting_triples})
-    if relation_strength == "strong" and support_count >= 2 and hedge_score < 0.34:
+    if hop_profiles and chunk_texts:
+        # Keep the existing chunk-context hedge signal, but apply it as a
+        # global maximum rather than letting one strong first hop hide a hedged
+        # second hop.
+        chunk_hedge = _hedge_score(chunk_texts[:1])
+        for profile in hop_profiles:
+            profile["hedge_score"] = max(float(profile.get("hedge_score", 0.0)), chunk_hedge)
+
+    relation_strengths = [str(profile["relation_strength"]) for profile in hop_profiles]
+    relation_strength = _weakest_strength(relation_strengths)
+    hedge_score = max((float(profile.get("hedge_score", 0.0)) for profile in hop_profiles), default=0.0)
+    hop_support_counts = [int(profile.get("support_count", 0) or 0) for profile in hop_profiles]
+    min_hop_support = min(hop_support_counts) if hop_support_counts else 0
+    support_count = independent_support_count(subgraph.supporting_triples)
+    doc_count = independent_doc_count(subgraph.supporting_triples)
+
+    all_hops_strong = all(strength == "strong" for strength in relation_strengths) and bool(relation_strengths)
+    if all_hops_strong and min_hop_support >= 2 and hedge_score < 0.34:
         evidence_strength = "strong"
-    elif relation_strength == "weak" or hedge_score >= 0.67:
+    elif relation_strength == "weak" or hedge_score >= 0.67 or min_hop_support == 0:
         evidence_strength = "weak"
     else:
         evidence_strength = "medium"
 
     if evidence_strength == "strong":
-        allowed_question_types = ["claim_choice", "boolean_support", "one_hop_tail", "essay", "experiment_code"]
-        if subgraph.question_type == "two_hop_tail" or len(subgraph.edges) > 1:
+        # boolean_support is intentionally disabled until reliable negative
+        # examples are generated; otherwise the boolean subset is positive-only.
+        allowed_question_types = ["claim_choice", "one_hop_tail", "essay", "experiment_code"]
+        if (subgraph.question_type == "two_hop_tail" or len(edges) > 1) and len(edges) == len(hop_profiles):
             allowed_question_types.append("two_hop_tail")
     elif evidence_strength == "medium":
-        allowed_question_types = ["claim_choice", "boolean_support", "one_hop_tail", "essay", "experiment_code"]
+        allowed_question_types = ["claim_choice", "one_hop_tail", "essay", "experiment_code"]
     else:
         # Weak evidence: skip fact-recall (one_hop_tail) since the chunk may not
         # actually commit to a specific tail — stick to hedged claim_choice / essay.
@@ -131,4 +209,7 @@ def profile_subgraph_evidence(subgraph) -> dict[str, object]:
         "preferred_question_type": preferred_question_type,
         "support_count": support_count,
         "doc_count": doc_count,
+        "min_hop_support_count": min_hop_support,
+        "hop_support_counts": hop_support_counts,
+        "hop_profiles": hop_profiles,
     }
diff --git a/fixed/question_generation/evidence_utils.py b/question_generation/evidence_utils.py
new file mode 100644
index 0000000..1bedff5
--- /dev/null
+++ b/question_generation/evidence_utils.py
@@ -0,0 +1,49 @@
+from __future__ import annotations
+
+from typing import Any
+
+from pubmed_graph.utils import normalize_text
+
+
+def evidence_source_key(record: Any) -> tuple[str, str, str]:
+    """Return a stable key for one independent evidence source.
+
+    The primary evidence unit is a local chunk, identified by ``(doc_id,
+    chunk_id)``. When either id is missing, fall back to the normalized evidence
+    text so duplicate extracted triples from the same span do not inflate
+    support counts.
+    """
+    doc_id = normalize_text(str(getattr(record, "doc_id", "") or ""))
+    chunk_id = normalize_text(str(getattr(record, "chunk_id", "") or ""))
+    if doc_id or chunk_id:
+        return (doc_id, chunk_id, "")
+    evidence = normalize_text(str(getattr(record, "evidence", "") or ""))
+    return ("", "", evidence[:1000])
+
+
+def independent_support_keys(records: list[Any]) -> set[tuple[str, str, str]]:
+    """Unique evidence-source keys for records with at least one usable key."""
+    keys: set[tuple[str, str, str]] = set()
+    for record in records:
+        key = evidence_source_key(record)
+        if any(key):
+            keys.add(key)
+    return keys
+
+
+def independent_support_count(records: list[Any]) -> int:
+    """Count independent supporting evidence sources, not extracted triple rows."""
+    return len(independent_support_keys(records))
+
+
+def independent_doc_count(records: list[Any]) -> int:
+    docs = {
+        normalize_text(str(getattr(record, "doc_id", "") or ""))
+        for record in records
+        if normalize_text(str(getattr(record, "doc_id", "") or ""))
+    }
+    return len(docs)
+
+
+def independent_chunk_count(records: list[Any]) -> int:
+    return len(independent_support_keys(records))
diff --git a/question_generation/experiment_generator.py b/question_generation/experiment_generator.py
index e680d74..9d12b65 100644
--- a/question_generation/experiment_generator.py
+++ b/question_generation/experiment_generator.py
@@ -9,6 +9,7 @@ from pubmed_graph.utils import normalize_text
 from .config import DEFAULT_GITHUB_SEARCH_LANGUAGE, DEFAULT_GITHUB_SEARCH_PER_PAGE
 from . import sandbox_runner
 from .code_relevance import select_relevant_functions_cached
+from .evidence_utils import independent_chunk_count, independent_doc_count, independent_support_count
 from .experiments import (
     BlueprintContext,
     ExperimentBlueprint,
@@ -66,6 +67,46 @@ def _build_incomplete_code(main_code: str, incomplete_functions: tuple[str, ...]
     return updated
 
 
+def _sanitize_identifier(value: str, fallback: str = "generic") -> str:
+    cleaned = normalize_text(value).replace(" ", "_").casefold()
+    cleaned = "".join(ch for ch in cleaned if ch.isalnum() or ch == "_").strip("_")
+    return cleaned or fallback
+
+
+def _blueprint_from_llm_spec(llm_spec: dict[str, Any], context: BlueprintContext) -> ExperimentBlueprint:
+    """Create a prompt/search blueprint from the accepted LLM spec itself.
+
+    The executable code and unit tests already come from ``llm_spec``. Using a
+    synthetic blueprint derived from the same spec keeps the question prompt and
+    GitHub reference queries aligned with the actual coding task instead of a
+    fallback template family.
+    """
+    task_family = str(llm_spec.get("task_family") or "llm_experiment")
+    task_objective = str(llm_spec.get("task_objective") or "Complete the evidence-grounded experiment code.")
+    research_focus = str(llm_spec.get("research_focus") or task_objective)
+    incomplete_functions = tuple(str(fn) for fn in (llm_spec.get("incomplete_functions") or ()))
+    github_query_seed = normalize_text(
+        f"{task_family} {task_objective} {context.head} {context.tail} python"
+    )
+    return ExperimentBlueprint(
+        name=f"llm_{_sanitize_identifier(task_family)}",
+        task_family=task_family,
+        relation=context.relation,
+        direction=str(llm_spec.get("research_direction") or f"llm_{_sanitize_identifier(context.relation)}"),
+        discipline=str(llm_spec.get("discipline") or "life"),
+        function_type=str(llm_spec.get("function_type") or "Data analysis"),
+        task_objective=task_objective,
+        research_focus=research_focus,
+        data_code_template=str(llm_spec.get("data_code", "")),
+        main_code_template=str(llm_spec.get("main_code", "")),
+        incomplete_functions=incomplete_functions,
+        hard_extra_blanks=(),
+        github_repo_query=str(llm_spec.get("github_repo_query") or github_query_seed),
+        github_code_query=str(llm_spec.get("github_code_query") or github_query_seed),
+        unit_tests=tuple(dict(item) for item in (llm_spec.get("unit_tests") or ())),
+    )
+
+
 # How much of each fetched file we put into the prompt. Stays well under
 # the prompt budget so a single experiment_code question stays manageable
 # even with multiple excerpts.
@@ -426,20 +467,25 @@ def _render_experiment_prompt(
 _EXP_LOGGER = __import__("logging").getLogger("question_generation.experiment")
 
 
-def _build_rejected_llm_sample(*, subgraph, sample_id: str, edge, reason: str) -> QuestionSample:
-    """Return a minimal ``QuestionSample`` pre-marked as rejected.
-
-    Used when ``generation_mode='llm'`` and the LLM path fails — we don't
-    want to silently fall back to the template, so we emit a sample whose
-    ``quality.validation_status='rejected'`` so the downstream validator
-    counts it as rejected and logs it with the given ``reason``.
-    """
+def _build_rejected_experiment_sample(
+    *,
+    subgraph,
+    sample_id: str,
+    reason: str,
+    generation_source: str,
+    blueprint_name: str,
+    validator_version: str = "experiment_generation_failed",
+) -> QuestionSample:
+    """Return a minimal experiment sample pre-marked as rejected."""
+    support_count = independent_support_count(subgraph.supporting_triples)
+    doc_support_count = independent_doc_count(subgraph.supporting_triples)
+    chunk_support_count = independent_chunk_count(subgraph.supporting_triples)
     stub_quality = Quality(
         validation_status="rejected",
         difficulty="hard",
         ambiguity_score=0.0,
         uniqueness_key=subgraph.uniqueness_key,
-        validator_version="llm_generation_failed",
+        validator_version=validator_version,
         rejection_reasons=[reason],
     )
     stub_grounding = Grounding(
@@ -447,11 +493,11 @@ def _build_rejected_llm_sample(*, subgraph, sample_id: str, edge, reason: str) -
         answer_supported=False,
         question_entities_supported=False,
         multi_hop_chain_supported=False,
-        supporting_evidence_count=len(subgraph.supporting_triples),
-        doc_support_count=len({t.doc_id for t in subgraph.supporting_triples}),
-        chunk_support_count=len({t.chunk_id for t in subgraph.supporting_triples}),
+        supporting_evidence_count=support_count,
+        doc_support_count=doc_support_count,
+        chunk_support_count=chunk_support_count,
         double_checked=False,
-        support_level="single_source",
+        support_level="multi_doc" if doc_support_count >= 2 else ("multi_chunk" if chunk_support_count >= 2 else "single_source"),
         validation_mode="rule_only",
         validation_status_detail=reason,
         evidence_strength="unknown",
@@ -462,7 +508,7 @@ def _build_rejected_llm_sample(*, subgraph, sample_id: str, edge, reason: str) -
     return QuestionSample(
         sample_id=sample_id,
         question_type="experiment_code",
-        question=f"[LLM generation failed: {reason}]",
+        question=f"[experiment generation rejected: {reason}]",
         answer=Answer(text="", canonical_text="", answer_type="Code"),
         options=[],
         subgraph={
@@ -477,14 +523,26 @@ def _build_rejected_llm_sample(*, subgraph, sample_id: str, edge, reason: str) -
         grounding=stub_grounding,
         quality=stub_quality,
         metadata={
-            "generation_source": "llm",
+            "generation_source": generation_source,
             "generation_failed_reason": reason,
-            "experiment_blueprint": "llm::failed",
+            "experiment_blueprint": blueprint_name,
             "experiment_difficulty": normalize_difficulty(subgraph.metadata.get("experiment_difficulty")),
         },
     )
 
 
+def _build_rejected_llm_sample(*, subgraph, sample_id: str, edge, reason: str) -> QuestionSample:
+    del edge
+    return _build_rejected_experiment_sample(
+        subgraph=subgraph,
+        sample_id=sample_id,
+        reason=reason,
+        generation_source="llm",
+        blueprint_name="llm::failed",
+        validator_version="llm_generation_failed",
+    )
+
+
 def build_experiment_sample(
     subgraph,
     sample_id: str,
@@ -550,19 +608,27 @@ def build_experiment_sample(
         difficulty=difficulty,
     )
     _t0 = _time.time()
-    blueprint_name, blueprint = dispatch_blueprint(blueprint_context)
-    _EXP_LOGGER.info("[%s] blueprint=%s dispatched in %.1fs", sample_id, blueprint_name, _time.time() - _t0)
-    # Pick code + unit_tests: LLM spec wins when present, else fall back
-    # to the hardcoded blueprint template. ``blueprint_name`` is kept for
-    # telemetry even when overridden by the LLM path.
     if llm_spec is not None:
-        data_code_text = llm_spec["data_code"]
-        main_code = llm_spec["main_code"]
-        incomplete_main_code = llm_spec["incomplete_main_code"]
-        unit_tests_list = [dict(ut) for ut in llm_spec["unit_tests"]]
-        blank_targets = tuple(llm_spec["incomplete_functions"])
+        blueprint = _blueprint_from_llm_spec(llm_spec, blueprint_context)
         blueprint_name = f"llm::{llm_spec.get('task_family', 'generic')}"
+        _EXP_LOGGER.info("[%s] blueprint=%s derived from LLM spec in %.1fs", sample_id, blueprint_name, _time.time() - _t0)
+        data_code_text = blueprint.data_code_template
+        main_code = blueprint.main_code_template
+        incomplete_main_code = str(llm_spec["incomplete_main_code"])
+        unit_tests_list = [dict(ut) for ut in blueprint.unit_tests]
+        blank_targets = tuple(blueprint.incomplete_functions)
     else:
+        try:
+            blueprint_name, blueprint = dispatch_blueprint(blueprint_context)
+        except RuntimeError:
+            return _build_rejected_experiment_sample(
+                subgraph=subgraph,
+                sample_id=sample_id,
+                reason="no_matching_experiment_blueprint",
+                generation_source="template",
+                blueprint_name="unmatched",
+            )
+        _EXP_LOGGER.info("[%s] blueprint=%s dispatched in %.1fs", sample_id, blueprint_name, _time.time() - _t0)
         data_code_text = blueprint.data_code_template
         main_code = blueprint.main_code_template
         blank_targets = select_blank_targets(blueprint, difficulty)
@@ -640,8 +706,9 @@ def build_experiment_sample(
         supporting_chunks=subgraph.supporting_chunks,
         source_docs=sorted({triple.doc_id for triple in subgraph.supporting_triples}),
     )
-    doc_support_count = len({triple.doc_id for triple in subgraph.supporting_triples})
-    chunk_support_count = len({triple.chunk_id for triple in subgraph.supporting_triples})
+    doc_support_count = independent_doc_count(subgraph.supporting_triples)
+    chunk_support_count = independent_chunk_count(subgraph.supporting_triples)
+    evidence_support_count = independent_support_count(subgraph.supporting_triples)
     support_level = "multi_doc" if doc_support_count >= 2 else ("multi_chunk" if chunk_support_count >= 2 else "single_source")
     evidence_profile = copy.deepcopy(subgraph.metadata.get("evidence_profile", {}))
     grounding = Grounding(
@@ -649,7 +716,7 @@ def build_experiment_sample(
         answer_supported=True,
         question_entities_supported=True,
         multi_hop_chain_supported=True,
-        supporting_evidence_count=len(subgraph.supporting_triples),
+        supporting_evidence_count=evidence_support_count,
         doc_support_count=doc_support_count,
         chunk_support_count=chunk_support_count,
         double_checked=False,
diff --git a/question_generation/experiments/blueprints/differential_expression.py b/question_generation/experiments/blueprints/differential_expression.py
index a7a44a9..4d0f4f1 100644
--- a/question_generation/experiments/blueprints/differential_expression.py
+++ b/question_generation/experiments/blueprints/differential_expression.py
@@ -16,17 +16,28 @@ def _factory(context: BlueprintContext) -> ExperimentBlueprint:
     disease = normalize_text(context.tail)
     relation = normalize_relation(context.relation)
     relation_label = relation.replace("_", " ")
+    downregulated = "down" in relation
+    if downregulated:
+        disease_values = (4.0, 5.0, 4.5, 5.5)
+        control_values = (13.0, 11.0, 12.5, 10.5)
+        expected_log2_fc = -1.3068
+        fold_change_test_name = "fold_change_negative"
+    else:
+        disease_values = (13.0, 11.0, 12.5, 10.5)
+        control_values = (4.0, 5.0, 4.5, 5.5)
+        expected_log2_fc = 1.3068
+        fold_change_test_name = "fold_change_positive"
     csv_text = csv_rows(
         [
             ("sample_id", "group", "expression"),
-            ("D1", "disease", 13.0),
-            ("D2", "disease", 11.0),
-            ("D3", "disease", 12.5),
-            ("D4", "disease", 10.5),
-            ("C1", "control", 4.0),
-            ("C2", "control", 5.0),
-            ("C3", "control", 4.5),
-            ("C4", "control", 5.5),
+            ("D1", "disease", disease_values[0]),
+            ("D2", "disease", disease_values[1]),
+            ("D3", "disease", disease_values[2]),
+            ("D4", "disease", disease_values[3]),
+            ("C1", "control", control_values[0]),
+            ("C2", "control", control_values[1]),
+            ("C3", "control", control_values[2]),
+            ("C4", "control", control_values[3]),
         ]
     )
     data_code = f'''#!/usr/bin/env python3
@@ -108,8 +119,9 @@ if __name__ == "__main__":
         github_repo_query=f"{gene} differential expression {disease} python",
         github_code_query=f"{gene} {disease} log2 fold change differential expression",
         unit_tests=(
-            # log2(11.75/4.75) ≈ 1.3068; verified by sandbox self-test.
-            {"name": "fold_change_positive", "input": {}, "expected_output": {"log2_fold_change": 1.3068}},
+            # The synthetic expression direction matches the relation:
+            # up/overexpressed claims expect positive log2FC; down claims expect negative log2FC.
+            {"name": fold_change_test_name, "input": {}, "expected_output": {"log2_fold_change": expected_log2_fc}},
             {"name": "verdict_supported", "input": {}, "expected_output": {"verdict": "supported"}},
         ),
     )
diff --git a/question_generation/experiments/blueprints/pathway_activity.py b/question_generation/experiments/blueprints/pathway_activity.py
index 500bd36..9936dc1 100644
--- a/question_generation/experiments/blueprints/pathway_activity.py
+++ b/question_generation/experiments/blueprints/pathway_activity.py
@@ -5,6 +5,28 @@ from pubmed_graph.utils import normalize_text
 from .._helpers import csv_rows
 from ..registry import REGISTRY, BlueprintContext, ExperimentBlueprint, normalize_relation
 
+_PATHWAY_RELATIONS = {
+    "associated_with",
+    "part_of",
+    "involved_in",
+    "involved_in_pathway",
+    "activates",
+    "inhibits",
+}
+_SUBJECT_TYPES = {"gene", "protein", "enzyme", "transcript", "mrna", "lncrna", "mirna"}
+_PATHWAY_TYPES = {"pathway", "biologicalprocess", "biological_process", "process"}
+
+
+def _type_key(node_type: str) -> str:
+    return (node_type or "").strip().lower().replace(" ", "").replace("_", "")
+
+
+def _predicate(context: BlueprintContext) -> bool:
+    relation = normalize_relation(context.relation)
+    if relation not in _PATHWAY_RELATIONS:
+        return False
+    return _type_key(context.head_type) in _SUBJECT_TYPES and _type_key(context.tail_type) in _PATHWAY_TYPES
+
 
 def _factory(context: BlueprintContext) -> ExperimentBlueprint:
     subject = normalize_text(context.head)
@@ -98,6 +120,4 @@ if __name__ == "__main__":
     )
 
 
-# Pathway activity is the explicit fallback — registered as a fallback rather
-# than via a wildcard predicate so the dispatch trace is honest.
-REGISTRY.register_fallback(name="pathway_activity", factory=_factory)
+REGISTRY.register(name="pathway_activity", predicate=_predicate, factory=_factory, priority=40)
diff --git a/question_generation/generator.py b/question_generation/generator.py
index 54251f7..419c449 100644
--- a/question_generation/generator.py
+++ b/question_generation/generator.py
@@ -6,6 +6,7 @@ import random
 from pubmed_graph.utils import normalize_text
 
 from .config import DEFAULT_DISTRACTOR_COUNT
+from .evidence_utils import evidence_source_key, independent_chunk_count, independent_doc_count, independent_support_count
 from .evidence_claims import build_claim_texts
 from .evidence_profiler import profile_subgraph_evidence
 from .experiment_generator import build_experiment_sample
@@ -45,6 +46,29 @@ def _claim_option_text(subgraph, head: str, relation: str, tail: str) -> str:
     return f"The evidence supports a contextual relationship in which {head} {relation.replace('_', ' ')} {tail}."
 
 
+def _relation_key(relation: str) -> str:
+    return normalize_text(relation).replace(" ", "_").casefold()
+
+
+def _claim_key(head: str, relation: str, tail: str) -> tuple[str, str, str]:
+    return (canonicalize_entity(head), _relation_key(relation), canonicalize_entity(tail))
+
+
+def _current_evidence_source_keys(subgraph) -> set[tuple[str, str, str]]:
+    keys = {evidence_source_key(triple) for triple in subgraph.supporting_triples}
+    for chunk in subgraph.supporting_chunks:
+        keys.add((normalize_text(chunk.doc_id), normalize_text(chunk.chunk_id), ""))
+    return {key for key in keys if any(key)}
+
+
+def _claim_supported_by_current_evidence(subgraph, head: str, relation: str, tail: str) -> bool:
+    key = _claim_key(head, relation, tail)
+    for triple in subgraph.supporting_triples:
+        if _claim_key(triple.head, triple.relation, triple.tail) == key:
+            return True
+    return False
+
+
 def _allowed_question_types(subgraph) -> set[str]:
     profile = subgraph.metadata.get("evidence_profile", {})
     return set(profile.get("allowed_question_types", [subgraph.question_type]))
@@ -54,39 +78,80 @@ def _build_options(index: QuestionGenerationIndex, subgraph, answer_text: str, a
     if subgraph.question_type == "claim_choice":
         edge = subgraph.edges[0]
         distractors: list[str] = []
+        option_claims: list[dict[str, object]] = []
         head_key = canonicalize_entity(edge.head)
         relation = edge.relation
-        local_claims = sorted(
+        correct_key = _claim_key(edge.head, relation, answer_text)
+        correct_text = _claim_option_text(subgraph, edge.head, relation, answer_text)
+        seen_claim_keys = {correct_key}
+        seen_text_keys = {normalize_text(correct_text).casefold()}
+        current_source_keys = _current_evidence_source_keys(subgraph)
+
+        option_claims.append(
             {
-                _claim_option_text(subgraph, edge.head, relation, triple.tail)
-                for triple in index.outgoing_by_head.get(head_key, [])
-                if triple.normalized_relation == relation and normalize_text(triple.tail) != normalize_text(answer_text)
+                "text": correct_text,
+                "head": edge.head,
+                "relation": relation,
+                "tail": answer_text,
+                "is_correct": True,
+                "supported_by_current_evidence": _claim_supported_by_current_evidence(subgraph, edge.head, relation, answer_text),
             }
         )
-        distractors.extend(local_claims)
-        correct_text = _claim_option_text(subgraph, edge.head, relation, answer_text)
-        for triple in index.triples:
-            if triple.normalized_relation != relation:
-                continue
+
+        def _try_add_distractor(triple) -> None:
+            if len(distractors) >= count:
+                return
+            if _relation_key(triple.normalized_relation) != _relation_key(relation):
+                return
+            claim_key = _claim_key(triple.head, relation, triple.tail)
+            if claim_key in seen_claim_keys:
+                return
+            # A distractor must not be another claim supported by the same
+            # evidence bundle, nor a claim extracted from the same local chunk.
+            if _claim_supported_by_current_evidence(subgraph, triple.head, relation, triple.tail):
+                return
+            if evidence_source_key(triple) in current_source_keys:
+                return
             claim = _claim_option_text(subgraph, triple.head, relation, triple.tail)
-            if normalize_text(claim) == normalize_text(correct_text):
-                continue
-            if claim in distractors:
-                continue
+            text_key = normalize_text(claim).casefold()
+            if text_key in seen_text_keys:
+                return
+            seen_claim_keys.add(claim_key)
+            seen_text_keys.add(text_key)
             distractors.append(claim)
+            option_claims.append(
+                {
+                    "text": claim,
+                    "head": triple.head,
+                    "relation": relation,
+                    "tail": triple.tail,
+                    "is_correct": False,
+                    "supported_by_current_evidence": False,
+                }
+            )
+
+        for triple in sorted(index.outgoing_by_head.get(head_key, []), key=lambda item: canonicalize_entity(item.tail)):
+            _try_add_distractor(triple)
             if len(distractors) >= count:
                 break
+        for triple in index.triples:
+            _try_add_distractor(triple)
+            if len(distractors) >= count:
+                break
+
+        subgraph.metadata["claim_choice_option_claims"] = option_claims
         options = [Option(text=correct_text, is_correct=True)]
         options.extend(Option(text=text, is_correct=False) for text in distractors[:count])
         return options
 
     distractors: list[str] = []
+    seen_entity_keys = {canonicalize_entity(answer_text)}
     type_pool = sorted(index.entities_by_type.get(answer_type, set()))
     for candidate in type_pool:
-        if normalize_text(candidate) == normalize_text(answer_text):
-            continue
-        if candidate in distractors:
+        candidate_key = canonicalize_entity(candidate)
+        if not candidate_key or candidate_key in seen_entity_keys:
             continue
+        seen_entity_keys.add(candidate_key)
         distractors.append(candidate)
         if len(distractors) >= count:
             break
@@ -235,15 +300,16 @@ def build_question_sample(
         supporting_chunks=subgraph.supporting_chunks,
         source_docs=sorted({triple.doc_id for triple in subgraph.supporting_triples}),
     )
-    doc_support_count = len({triple.doc_id for triple in subgraph.supporting_triples})
-    chunk_support_count = len({triple.chunk_id for triple in subgraph.supporting_triples})
+    doc_support_count = independent_doc_count(subgraph.supporting_triples)
+    chunk_support_count = independent_chunk_count(subgraph.supporting_triples)
+    evidence_support_count = independent_support_count(subgraph.supporting_triples)
     support_level = "multi_doc" if doc_support_count >= 2 else ("multi_chunk" if chunk_support_count >= 2 else "single_source")
     grounding = Grounding(
         is_fully_grounded=True,
         answer_supported=True,
         question_entities_supported=True,
-        multi_hop_chain_supported=subgraph.question_type == "two_hop_tail" or len(subgraph.edges) == 1,
-        supporting_evidence_count=len(subgraph.supporting_triples),
+        multi_hop_chain_supported=subgraph.question_type != "two_hop_tail" or len(subgraph.edges) >= 2,
+        supporting_evidence_count=evidence_support_count,
         doc_support_count=doc_support_count,
         chunk_support_count=chunk_support_count,
         double_checked=False,
@@ -255,7 +321,7 @@ def build_question_sample(
         question_type_allowed_by_evidence=subgraph.question_type in set(evidence_profile.get("allowed_question_types", [])),
         evidence_profile_version="v1",
     )
-    support_count = len(subgraph.supporting_triples)
+    support_count = evidence_support_count
     quality = Quality(
         validation_status="pending",
         difficulty="hard" if subgraph.question_type == "two_hop_tail" else ("medium" if support_count > 1 else "easy"),
diff --git a/question_generation/model_validator.py b/question_generation/model_validator.py
index 8299145..7c4f26b 100644
--- a/question_generation/model_validator.py
+++ b/question_generation/model_validator.py
@@ -1,5 +1,7 @@
 from __future__ import annotations
 
+import hashlib
+import json
 from typing import Any
 
 from pubmed_graph.llm import InternChatClient
@@ -156,13 +158,23 @@ def judge_experiment_code(
     ``issue_tags`` like ``code_off_topic`` / ``direction_flipped`` /
     ``blank_too_trivial``.
     """
+    evidence_payload = {
+        "evidence_snippet": evidence_snippet,
+        "evidence_bundle": evidence_bundle,
+    }
+    evidence_digest = hashlib.sha1(
+        json.dumps(evidence_payload, sort_keys=True, default=str).encode("utf-8")
+    ).hexdigest()
     payload = {
         "type": "experiment_judge",
         "scientific_claim": scientific_claim,
-        # Only hash what affects the verdict — truncated so paraphrases hit cache
+        # Only hash what affects the verdict — truncated so paraphrases hit cache.
+        # The evidence digest is included because the judge explicitly evaluates
+        # whether the evidence direction/sign matches the code.
         "main_code_head": (main_code or "")[:3500],
         "unit_tests": unit_tests,
         "incomplete_functions": list(incomplete_functions),
+        "evidence_digest": evidence_digest,
         "model": model_config.get("model", ""),
     }
     if cache:
diff --git a/question_generation/retrieval_validator.py b/question_generation/retrieval_validator.py
index 2ff6455..17ade8a 100644
--- a/question_generation/retrieval_validator.py
+++ b/question_generation/retrieval_validator.py
@@ -24,9 +24,34 @@ RELATION_QUERY_HINTS = {
 }
 
 
+def _relation_text(relation: str) -> str:
+    return normalize_text(relation).replace("_", " ")
+
+
+def _two_hop_chain_claim(sample) -> str:
+    edges = sample.subgraph.get("edges", []) if isinstance(sample.subgraph, dict) else []
+    if len(edges) < 2:
+        return ""
+    first, second = edges[0], edges[1]
+    rel1 = _relation_text(str(first.get("relation", "")))
+    rel2 = _relation_text(str(second.get("relation", "")))
+    intermediate = str(first.get("tail", ""))
+    return (
+        "The evidence supports a complete two-hop chain: "
+        f"{first.get('head', '')} {rel1} {intermediate}, and "
+        f"{intermediate} {rel2} {second.get('tail', '')}. "
+        f"The unique supported intermediate entity is {intermediate}."
+    )
+
+
 def build_claim_text(sample) -> str:
     if sample.question_type == "claim_choice":
         return sample.answer.text
+    if sample.question_type == "two_hop_tail":
+        # Always construct the chain claim from the actual sample edges/answer so
+        # the judge validates both hops and the labelled intermediate entity.
+        # Metadata claim text may be conservative prose and may omit uniqueness.
+        return _two_hop_chain_claim(sample)
     if sample.subgraph.get("edges"):
         edge = sample.subgraph["edges"][0]
         return f"{edge['head']} {edge['relation']} {edge['tail']}"
@@ -37,6 +62,15 @@ def build_claim_query(sample) -> str:
     query_text = normalize_text(sample.metadata.get("query_text", ""))
     if query_text:
         return query_text
+    if sample.question_type == "two_hop_tail":
+        edges = sample.subgraph.get("edges", []) if isinstance(sample.subgraph, dict) else []
+        if len(edges) >= 2:
+            first, second = edges[0], edges[1]
+            rel1 = normalize_text(first.get("relation", "")).replace(" ", "_").casefold()
+            rel2 = normalize_text(second.get("relation", "")).replace(" ", "_").casefold()
+            hint1 = RELATION_QUERY_HINTS.get(rel1, rel1.replace("_", " "))
+            hint2 = RELATION_QUERY_HINTS.get(rel2, rel2.replace("_", " "))
+            return normalize_text(f"{first.get('head', '')} {first.get('tail', '')} {second.get('tail', '')} {hint1} {hint2}")
     if sample.subgraph.get("edges"):
         edge = sample.subgraph["edges"][0]
         relation = normalize_text(edge.get("relation", "")).replace(" ", "_").casefold()
diff --git a/question_generation/sampler.py b/question_generation/sampler.py
index a79987c..95ec043 100644
--- a/question_generation/sampler.py
+++ b/question_generation/sampler.py
@@ -4,8 +4,11 @@ import random
 
 from .config import DEFAULT_MAX_SAMPLES, DEFAULT_MIN_CONFIDENCE, DEFAULT_MIN_SUPPORT
 from .evidence_profiler import profile_subgraph_evidence
+from pubmed_graph.utils import normalize_text
+
+from .evidence_utils import independent_support_count
 from .experiments import DEFAULT_DIFFICULTY
-from .indexing import QuestionGenerationIndex
+from .indexing import QuestionGenerationIndex, canonicalize_entity
 from .subgraph_builder import build_single_edge_subgraph, build_two_hop_subgraph, build_vqa_subgraph
 
 
@@ -33,8 +36,8 @@ def sample_single_hop_subgraphs(
         if not triples:
             continue
         first = triples[0]
-        support_count = len(triples)
-        avg_conf = sum(float(triple.confidence) for triple in triples) / support_count
+        support_count = independent_support_count(triples)
+        avg_conf = sum(float(triple.confidence) for triple in triples) / max(len(triples), 1)
         if avg_conf < min_confidence or support_count < min_support:
             continue
         if "claim_choice" in question_types:
@@ -130,20 +133,24 @@ def sample_two_hop_subgraphs(
         # bypassed the gate entirely, which made two_hop_tail dominate
         # output (e.g. 1225/1368 on graphbench). Callers that want the
         # prior lax behaviour can pass min_support=1.
-        if not first_hop or len(first_hop) < min_support:
+        if not first_hop or independent_support_count(first_hop) < min_support:
             continue
         pivot = first_hop[0].tail
-        second_hops = index.outgoing_by_head.get(pivot.casefold(), [])
+        pivot_key = canonicalize_entity(pivot)
+        second_hops = index.outgoing_by_head.get(pivot_key, [])
         if not second_hops:
             continue
         grouped: dict[tuple[str, str, str], list] = {}
         for triple in second_hops:
-            if triple.tail.casefold() == first_hop[0].head.casefold():
+            if canonicalize_entity(triple.head) != pivot_key:
+                continue
+            if canonicalize_entity(triple.tail) == canonicalize_entity(first_hop[0].head):
                 continue
-            group_key = (triple.head.casefold(), triple.normalized_relation, triple.tail.casefold())
+            relation_key = normalize_text(triple.normalized_relation).replace(" ", "_").casefold()
+            group_key = (canonicalize_entity(triple.head), relation_key, canonicalize_entity(triple.tail))
             grouped[group_key] = grouped.get(group_key, []) + [triple]
         for second_group in grouped.values():
-            if len(second_group) < min_support:
+            if independent_support_count(second_group) < min_support:
                 continue
             left_conf = sum(float(t.confidence) for t in first_hop) / len(first_hop)
             right_conf = sum(float(t.confidence) for t in second_group) / len(second_group)
diff --git a/question_generation/sampling_plan.py b/question_generation/sampling_plan.py
index 1576459..c7ee5e7 100644
--- a/question_generation/sampling_plan.py
+++ b/question_generation/sampling_plan.py
@@ -120,16 +120,43 @@ def ratios_to_quotas(
     ratios: dict[str, float],
     total_quota: int,
 ) -> dict[str, int]:
-    """Round ratio×total to integer quotas. Fix up rounding drift so the
-    quotas sum to exactly ``total_quota`` by nudging the largest ratio.
+    """Convert ratios to integer quotas with largest-remainder apportionment.
+
+    The returned quotas always sum to ``total_quota`` when at least one ratio
+    key is present, avoiding the drift/truncation bug from round-then-nudge.
     """
-    raw = {t: max(0.0, ratios.get(t, 0.0)) * total_quota for t in ratios}
-    quotas = {t: int(round(v)) for t, v in raw.items()}
-    drift = total_quota - sum(quotas.values())
-    if drift != 0 and quotas:
-        # Move drift onto the key with the largest raw value.
-        pivot = max(raw, key=lambda k: raw[k])
-        quotas[pivot] = max(0, quotas[pivot] + drift)
+    if not ratios:
+        return {}
+    target = max(0, int(total_quota))
+    if target == 0:
+        return {t: 0 for t in ratios}
+
+    weights = {t: max(0.0, ratios.get(t, 0.0)) for t in ratios}
+    weight_sum = sum(weights.values())
+    if weight_sum <= 0:
+        weights = {t: 1.0 for t in ratios}
+        weight_sum = float(len(weights))
+
+    raw = {t: (weights[t] / weight_sum) * target for t in ratios}
+    quotas = {t: int(raw[t]) for t in ratios}
+    remaining = target - sum(quotas.values())
+    if remaining > 0:
+        order = sorted(
+            ratios,
+            key=lambda t: (raw[t] - int(raw[t]), raw[t], t),
+            reverse=True,
+        )
+        for t in order[:remaining]:
+            quotas[t] += 1
+    elif remaining < 0:
+        order = sorted(
+            ratios,
+            key=lambda t: (raw[t] - int(raw[t]), raw[t], t),
+        )
+        for t in order[: -remaining]:
+            quotas[t] = max(0, quotas[t] - 1)
+
+    assert sum(quotas.values()) == target
     return quotas
 
 
@@ -720,9 +747,6 @@ def _greedy_pick_cross_type(
             if gain > best_gain:
                 best_gain = gain
                 best_i = i
-                if best_gain == 0:
-                    # Zero-gain candidate — accept; more scanning wouldn't help.
-                    break
         if best_i < 0:
             break
         qtype, idx, n_set, e_set = tagged.pop(best_i)
@@ -779,9 +803,6 @@ def _greedy_pick(
             if gain > best_gain:
                 best_gain = gain
                 best_i = pos
-                if best_gain == 0:
-                    # Zero-gain candidate is acceptable; stop early for speed
-                    break
         if best_i < 0:
             break
         chosen_idx = remaining_idx.pop(best_i)
diff --git a/question_generation/sandbox_runner.py b/question_generation/sandbox_runner.py
index 662e707..d987e89 100644
--- a/question_generation/sandbox_runner.py
+++ b/question_generation/sandbox_runner.py
@@ -85,8 +85,10 @@ def _values_match(actual, expected):
 
 
 def _pick_callable(name_hint: str):
-    if name_hint and hasattr(_main_module, name_hint):
-        return getattr(_main_module, name_hint)
+    if name_hint:
+        if hasattr(_main_module, name_hint):
+            return getattr(_main_module, name_hint)
+        raise AttributeError(f"explicit unit-test function not found: {name_hint}")
     for attr in dir(_main_module):
         if attr.startswith("summarize_"):
             return getattr(_main_module, attr)
@@ -250,12 +252,13 @@ def evaluate_experiment_sample(
             "incomplete":           {... run_unit_tests output ...},
             "reference_passes_all": bool,
             "incomplete_fails_some": bool,
-            "verdict": "passed" | "rejected" | "skipped",
+            "verdict": "passed" | "rejected",
             "rejection_reasons":   list[str],
         }
 
-    ``verdict == 'skipped'`` means the sandbox was unavailable; the validator
-    treats that as a soft pass and falls back to the prior rule-only behaviour.
+    The sandbox verdict fails closed: unavailable sandboxes, missing tests,
+    timeouts, and harness errors are rejected because the code label cannot be
+    trusted without a completed runtime check.
     """
     if not unit_tests:
         return {
@@ -263,8 +266,8 @@ def evaluate_experiment_sample(
             "incomplete": {"sandbox_status": "no_results", "passed": 0, "failed": 0, "total": 0, "test_results": []},
             "reference_passes_all": False,
             "incomplete_fails_some": False,
-            "verdict": "skipped",
-            "rejection_reasons": [],
+            "verdict": "rejected",
+            "rejection_reasons": ["missing_unit_tests"],
         }
 
     if not sandbox_client.is_sandbox_available():
@@ -296,8 +299,8 @@ def evaluate_experiment_sample(
             },
             "reference_passes_all": False,
             "incomplete_fails_some": False,
-            "verdict": "skipped",
-            "rejection_reasons": [],
+            "verdict": "rejected",
+            "rejection_reasons": ["sandbox_unavailable"],
         }
 
     reference = run_unit_tests(
@@ -319,15 +322,20 @@ def evaluate_experiment_sample(
         and reference["failed"] == 0
         and not reference.get("compile_error")
     )
-    # The masked prompt is allowed to either fail tests *or* fail to import
-    # at all (NameError on the blanked-out function) — both prove the blank
-    # is meaningful. We do require the sandbox call itself to have completed.
+    # The masked prompt must still complete the harness and then fail at least
+    # one unit test (or be caught by the harness as a main-code compile error).
+    # A sandbox/harness execution_error means the validation itself failed and
+    # must be rejected fail-closed, not counted as a meaningful blank.
     incomplete_meaningful_blank = (
-        incomplete["sandbox_status"] in {"ok", "execution_error"}
+        incomplete["sandbox_status"] == "ok"
         and (incomplete["failed"] > 0 or incomplete.get("compile_error"))
     )
 
     rejection_reasons: list[str] = []
+    if reference["sandbox_status"] != "ok":
+        rejection_reasons.append(f"reference_sandbox_status_{reference['sandbox_status']}")
+    if incomplete["sandbox_status"] != "ok":
+        rejection_reasons.append(f"incomplete_sandbox_status_{incomplete['sandbox_status']}")
     if not reference_passes_all:
         rejection_reasons.append("reference_solution_failed_unit_tests")
     if not incomplete_meaningful_blank:
diff --git a/question_generation/subgraph_builder.py b/question_generation/subgraph_builder.py
index a77738a..09fe985 100644
--- a/question_generation/subgraph_builder.py
+++ b/question_generation/subgraph_builder.py
@@ -4,6 +4,7 @@ from collections import defaultdict
 
 from pubmed_graph.models import TripleRecord
 
+from .evidence_utils import independent_doc_count, independent_support_count
 from .indexing import QuestionGenerationIndex, canonicalize_entity
 from .models import SampledSubgraph, SubgraphEdge, SubgraphNode, SupportingChunk, SupportingTriple
 
@@ -23,12 +24,12 @@ def aggregate_support(triples: list[TripleRecord], index: QuestionGenerationInde
         )
         for triple in triples
     ]
-    chunks_by_id: dict[str, SupportingChunk] = {}
+    chunks_by_id: dict[tuple[str, str], SupportingChunk] = {}
     for triple in triples:
         chunk = index.chunks_by_id.get(triple.chunk_id)
         if chunk is None:
             continue
-        chunks_by_id[chunk.chunk_id] = SupportingChunk(
+        chunks_by_id[(chunk.doc_id, chunk.chunk_id)] = SupportingChunk(
             doc_id=chunk.doc_id,
             chunk_id=chunk.chunk_id,
             title=chunk.title,
@@ -56,7 +57,7 @@ def build_single_edge_subgraph(
             relation=first.normalized_relation,
             tail=first.tail,
             aggregated_confidence=avg_confidence,
-            support_count=len(triples),
+            support_count=independent_support_count(triples),
         )
     ]
     if question_type in {"single_hop_tail", "one_hop_tail"}:
@@ -90,7 +91,10 @@ def build_single_edge_subgraph(
         uniqueness_key="|".join([question_type, canonicalize_entity(first.head), first.normalized_relation, canonicalize_entity(first.tail)]),
         supporting_triples=supporting_triples,
         supporting_chunks=supporting_chunks,
-        metadata={"support_count": len(triples), "doc_count": len({triple.doc_id for triple in triples})},
+        metadata={
+            "support_count": independent_support_count(triples),
+            "doc_count": independent_doc_count(triples),
+        },
     )
 
 
@@ -138,9 +142,19 @@ def build_two_hop_subgraph(
 ) -> SampledSubgraph:
     left = first_hop[0]
     right = second_hop[0]
+    if canonicalize_entity(left.tail) != canonicalize_entity(right.head):
+        raise ValueError(
+            "two-hop subgraph is not connected after entity canonicalization: "
+            f"{left.head!r} -> {left.tail!r} / {right.head!r} -> {right.tail!r}"
+        )
     supporting_triples_left, supporting_chunks_left, left_conf = aggregate_support(first_hop, index)
     supporting_triples_right, supporting_chunks_right, right_conf = aggregate_support(second_hop, index)
-    chunk_map = {chunk.chunk_id: chunk for chunk in [*supporting_chunks_left, *supporting_chunks_right]}
+    chunk_map = {
+        (chunk.doc_id, chunk.chunk_id): chunk
+        for chunk in [*supporting_chunks_left, *supporting_chunks_right]
+    }
+    left_support_count = independent_support_count(first_hop)
+    right_support_count = independent_support_count(second_hop)
     return SampledSubgraph(
         nodes=[
             SubgraphNode(id=left.head, node_type=left.head_type),
@@ -148,8 +162,8 @@ def build_two_hop_subgraph(
             SubgraphNode(id=right.tail, node_type=right.tail_type),
         ],
         edges=[
-            SubgraphEdge(left.head, left.normalized_relation, left.tail, left_conf, len(first_hop)),
-            SubgraphEdge(right.head, right.normalized_relation, right.tail, right_conf, len(second_hop)),
+            SubgraphEdge(left.head, left.normalized_relation, left.tail, left_conf, left_support_count),
+            SubgraphEdge(right.head, right.normalized_relation, right.tail, right_conf, right_support_count),
         ],
         question_type="two_hop_tail",
         target_answer=left.tail,
@@ -166,5 +180,10 @@ def build_two_hop_subgraph(
         ]),
         supporting_triples=[*supporting_triples_left, *supporting_triples_right],
         supporting_chunks=list(chunk_map.values()),
-        metadata={"support_count": len(first_hop) + len(second_hop), "doc_count": len({triple.doc_id for triple in [*first_hop, *second_hop]})},
+        metadata={
+            "support_count": independent_support_count([*first_hop, *second_hop]),
+            "doc_count": independent_doc_count([*first_hop, *second_hop]),
+            "hop_support_counts": [left_support_count, right_support_count],
+            "hop_doc_counts": [independent_doc_count(first_hop), independent_doc_count(second_hop)],
+        },
     )
diff --git a/question_generation/templates.py b/question_generation/templates.py
index a66ee80..f176409 100644
--- a/question_generation/templates.py
+++ b/question_generation/templates.py
@@ -54,9 +54,8 @@ def render_question(subgraph: SampledSubgraph) -> str:
                 f"and explain what the available evidence indicates about their interaction."
             )
         if evidence_strength == "strong":
-            phrase_verb = _relation_phrase(edge.relation, fallback_subject_first=True).removeprefix("is reported to ")
             return (
-                f"{prefix}explain the mechanism by which {edge.head} {phrase_verb} {edge.tail}, "
+                f"{prefix}summarize the evidence-supported relationship between {edge.head} and {edge.tail}, "
                 f"citing the supporting evidence."
             )
         return (
diff --git a/question_generation/validation_prompts.py b/question_generation/validation_prompts.py
index fc4dcba..f07ed10 100644
--- a/question_generation/validation_prompts.py
+++ b/question_generation/validation_prompts.py
@@ -13,7 +13,7 @@ Return JSON only with these keys:
 - verdict: "supported" | "insufficient_evidence" | "contradicted"
 - score: float 0.0-1.0 (overall quality score)
 - rationale: brief explanation of your judgment
-- issue_tags: list of any issues found (e.g. "unsupported_claim", "missing_mechanism", "overstated")
+- issue_tags: list of any issues found (e.g. "unsupported_claim", "missing_relevant_evidence", "overstated")
 """
 
 
diff --git a/question_generation/validator.py b/question_generation/validator.py
index b80d1ee..16672cb 100644
--- a/question_generation/validator.py
+++ b/question_generation/validator.py
@@ -8,6 +8,7 @@ from pubmed_graph.utils import normalize_text
 
 from .config import DEFAULT_MIN_DOUBLE_CHECK_SUPPORT
 from .corroboration_agent import CorroborationAgent, CorroborationResult
+from .evidence_utils import independent_chunk_count, independent_doc_count, independent_support_count
 from .indexing import QuestionGenerationIndex, canonicalize_entity
 from .model_validator import judge_claim, judge_essay, judge_experiment_code
 from .retrieval_validator import build_claim_text, retrieve_evidence_bundle, serialize_evidence_bundle
@@ -24,14 +25,53 @@ ALLOWED_TYPE_PATTERNS = {
     ("Protein", "associated_with", "Pathway"),
     ("Protein", "part_of", "Pathway"),
     ("Drug", "inhibits", "Protein"),
+    ("Drug", "inhibits", "Gene"),
     ("Drug", "improves", "Disease"),
     ("Biomarker", "associated_with", "Disease"),
     ("Pathway", "associated_with", "Disease"),
+    ("Gene", "associated_with", "Disease"),
+    ("Protein", "associated_with", "Disease"),
     ("Gene", "overexpressed_in", "Disease"),
     ("Gene", "upregulated_in", "Disease"),
     ("Gene", "downregulated_in", "Disease"),
+    ("Gene", "activates", "Pathway"),
+    ("Protein", "activates", "Pathway"),
+    ("Gene", "inhibits", "Pathway"),
+    ("Protein", "inhibits", "Pathway"),
+    ("Gene", "activates", "Protein"),
+    ("Protein", "activates", "Protein"),
+    ("Gene", "inhibits", "Protein"),
+    ("Protein", "inhibits", "Protein"),
 }
 
+_TYPE_ALIASES = {
+    "gene": "Gene",
+    "genes": "Gene",
+    "protein": "Protein",
+    "proteins": "Protein",
+    "drug": "Drug",
+    "compound": "Drug",
+    "chemical": "Drug",
+    "biomarker": "Biomarker",
+    "pathway": "Pathway",
+    "disease": "Disease",
+    "condition": "Disease",
+    "biologicalprocess": "BiologicalProcess",
+    "biological_process": "BiologicalProcess",
+    "process": "BiologicalProcess",
+}
+
+_NORMALIZED_ALLOWED_TYPE_PATTERNS = {
+    (
+        normalize_text(head_type).casefold(),
+        normalize_text(relation).replace(" ", "_").casefold(),
+        normalize_text(tail_type).casefold(),
+    )
+    for head_type, relation, tail_type in ALLOWED_TYPE_PATTERNS
+}
+
+_ONTOLOGY_DOMAIN_RANGE_CACHE: dict[str, tuple[set[str], set[str]]] | None = None
+
 
 def _token_overlap_ratio(evidence: str, chunk_text: str) -> float:
     """Fraction of evidence word-tokens (>=3 chars) present in chunk_text.
@@ -52,6 +92,207 @@ def _token_overlap_ratio(evidence: str, chunk_text: str) -> float:
 _EVIDENCE_TOKEN_OVERLAP_THRESHOLD = 0.8
 
 
+def _relation_key(relation: str) -> str:
+    return normalize_text(relation).replace(" ", "_").casefold()
+
+
+def _canonical_type_name(node_type: str) -> str:
+    raw = normalize_text(str(node_type or ""))
+    compact = raw.replace(" ", "").replace("_", "").casefold()
+    return _TYPE_ALIASES.get(compact, raw)
+
+
+def _node_type_lookup(sample: QuestionSample) -> dict[str, str]:
+    nodes = sample.subgraph.get("nodes", []) if isinstance(sample.subgraph, dict) else []
+    out: dict[str, str] = {}
+    for node in nodes:
+        if not isinstance(node, dict):
+            continue
+        node_id = canonicalize_entity(str(node.get("id", "")))
+        if node_id:
+            out[node_id] = _canonical_type_name(str(node.get("node_type", "")))
+    return out
+
+
+def _as_iterable_values(value: Any) -> list[Any]:
+    if not value:
+        return []
+    if isinstance(value, str):
+        return [value]
+    try:
+        return list(value)
+    except TypeError:
+        return [value]
+
+
+def _ontology_domain_ranges() -> dict[str, tuple[set[str], set[str]]]:
+    global _ONTOLOGY_DOMAIN_RANGE_CACHE
+    if _ONTOLOGY_DOMAIN_RANGE_CACHE is not None:
+        return _ONTOLOGY_DOMAIN_RANGE_CACHE
+    try:
+        from pubmed_graph.graph_builder import OntologyProposerAgent  # type: ignore
+
+        ontology = OntologyProposerAgent().get_current_ontology()
+    except Exception:
+        _ONTOLOGY_DOMAIN_RANGE_CACHE = {}
+        return _ONTOLOGY_DOMAIN_RANGE_CACHE
+
+    out: dict[str, tuple[set[str], set[str]]] = {}
+    for relation in getattr(ontology, "relations", []) or []:
+        name = _relation_key(getattr(relation, "name", ""))
+        if not name:
+            continue
+        domain_values = getattr(relation, "domain", None) or getattr(relation, "domains", None) or []
+        range_values = getattr(relation, "range", None) or getattr(relation, "ranges", None) or []
+        domains = {
+            _canonical_type_name(str(value))
+            for value in _as_iterable_values(domain_values)
+            if str(value or "").strip()
+        }
+        ranges = {
+            _canonical_type_name(str(value))
+            for value in _as_iterable_values(range_values)
+            if str(value or "").strip()
+        }
+        if domains and ranges:
+            out[name] = (domains, ranges)
+    _ONTOLOGY_DOMAIN_RANGE_CACHE = out
+    return out
+
+
+def _edge_type_allowed(head_type: str, relation: str, tail_type: str) -> bool:
+    head = _canonical_type_name(head_type)
+    tail = _canonical_type_name(tail_type)
+    rel = _relation_key(relation)
+    if not head or not tail or not rel:
+        return False
+    ontology_ranges = _ontology_domain_ranges().get(rel)
+    if ontology_ranges:
+        allowed_domains, allowed_ranges = ontology_ranges
+        return head in allowed_domains and tail in allowed_ranges
+    return (normalize_text(head).casefold(), rel, normalize_text(tail).casefold()) in _NORMALIZED_ALLOWED_TYPE_PATTERNS
+
+
+def _edge_supporting_triples(sample: QuestionSample, edge: dict[str, Any]) -> list[Any]:
+    head = canonicalize_entity(str(edge.get("head", "")))
+    relation = _relation_key(str(edge.get("relation", "")))
+    tail = canonicalize_entity(str(edge.get("tail", "")))
+    matches: list[Any] = []
+    for triple in getattr(sample.provenance, "supporting_triples", []) or []:
+        if canonicalize_entity(getattr(triple, "head", "")) != head:
+            continue
+        if _relation_key(getattr(triple, "relation", "")) != relation:
+            continue
+        if canonicalize_entity(getattr(triple, "tail", "")) != tail:
+            continue
+        matches.append(triple)
+    return matches
+
+
+def _refresh_independent_grounding_counts(sample: QuestionSample) -> None:
+    triples = list(getattr(sample.provenance, "supporting_triples", []) or [])
+    if not triples:
+        return
+    sample.grounding.supporting_evidence_count = independent_support_count(triples)
+    sample.grounding.doc_support_count = independent_doc_count(triples)
+    sample.grounding.chunk_support_count = independent_chunk_count(triples)
+    if sample.grounding.doc_support_count >= 2:
+        sample.grounding.support_level = "multi_doc"
+    elif sample.grounding.chunk_support_count >= 2:
+        sample.grounding.support_level = "multi_chunk"
+    else:
+        sample.grounding.support_level = "single_source"
+
+
+def _multi_hop_chain_supported_by_evidence(sample: QuestionSample) -> bool:
+    if sample.question_type != "two_hop_tail":
+        return True
+    edges = sample.subgraph.get("edges", []) if isinstance(sample.subgraph, dict) else []
+    if len(edges) < 2:
+        return False
+    first, second = edges[0], edges[1]
+    intermediate = canonicalize_entity(str(sample.answer.text))
+    if canonicalize_entity(str(first.get("tail", ""))) != intermediate:
+        return False
+    if canonicalize_entity(str(second.get("head", ""))) != intermediate:
+        return False
+    if canonicalize_entity(str(first.get("tail", ""))) != canonicalize_entity(str(second.get("head", ""))):
+        return False
+    for edge in (first, second):
+        if independent_support_count(_edge_supporting_triples(sample, edge)) < DEFAULT_MIN_DOUBLE_CHECK_SUPPORT:
+            return False
+    return True
+
+
+def _claim_supported_by_sample_evidence(sample: QuestionSample, claim: dict[str, object]) -> bool:
+    head = canonicalize_entity(str(claim.get("head", "")))
+    relation = _relation_key(str(claim.get("relation", "")))
+    tail = canonicalize_entity(str(claim.get("tail", "")))
+    for triple in getattr(sample.provenance, "supporting_triples", []) or []:
+        if canonicalize_entity(getattr(triple, "head", "")) != head:
+            continue
+        if _relation_key(getattr(triple, "relation", "")) != relation:
+            continue
+        if canonicalize_entity(getattr(triple, "tail", "")) != tail:
+            continue
+        return True
+    return False
+
+
+def _claim_choice_options_valid(sample: QuestionSample) -> bool:
+    if sample.question_type != "claim_choice":
+        return True
+    if not sample.options or len(sample.options) < 2:
+        return False
+    correct_options = [option for option in sample.options if option.is_correct]
+    if len(correct_options) != 1:
+        return False
+    if normalize_text(correct_options[0].text).casefold() != normalize_text(sample.answer.text).casefold():
+        return False
+    claims = sample.metadata.get("claim_choice_option_claims", []) if isinstance(sample.metadata, dict) else []
+    if not isinstance(claims, list):
+        return False
+    claims_by_text = {
+        normalize_text(str(claim.get("text", ""))).casefold(): claim
+        for claim in claims
+        if isinstance(claim, dict) and normalize_text(str(claim.get("text", "")))
+    }
+    if len(claims_by_text) < len(sample.options):
+        return False
+    for option in sample.options:
+        key = normalize_text(option.text).casefold()
+        claim = claims_by_text.get(key)
+        if claim is None:
+            return False
+        actual_supported = _claim_supported_by_sample_evidence(sample, claim)
+        declared_supported = bool(claim.get("supported_by_current_evidence"))
+        if option.is_correct:
+            if not (actual_supported and bool(claim.get("is_correct"))):
+                return False
+        else:
+            # Multiple supported claims make the question multi-answer; reject.
+            if actual_supported or declared_supported or bool(claim.get("is_correct")):
+                return False
+    return True
+
+
+def _claim_choice_judge_question(sample: QuestionSample) -> str:
+    options = "\n".join(f"- {option.text}" for option in sample.options)
+    return (
+        f"{sample.question}\n\nCandidate options:\n{options}\n\n"
+        "Validate that the selected answer is the only option directly supported by the evidence. "
+        "Reject the sample if any non-selected option is also supported."
+    )
+
+
+def _two_hop_judge_question(sample: QuestionSample) -> str:
+    chain_claim = build_claim_text(sample)
+    return (
+        f"{sample.question}\n\nComplete chain claim to validate: {chain_claim}\n"
+        "Validate both hops of the chain and the uniqueness of the intermediate answer."
+    )
+
+
 def _evidence_supported(index: QuestionGenerationIndex, sample: QuestionSample) -> bool:
     # experiment_code samples are grounded through the sandbox harness
     # (reference solution passes unit tests), not through textual evidence
@@ -86,6 +327,9 @@ def _supports_minimum_double_check(sample: QuestionSample) -> bool:
     # (their grounding is image / sandbox respectively, not chunk overlap).
     if sample.question_type in {"vqa", "experiment_code"}:
         return True
+    _refresh_independent_grounding_counts(sample)
+    if sample.question_type == "two_hop_tail":
+        return _multi_hop_chain_supported_by_evidence(sample)
     return (
         sample.grounding.supporting_evidence_count >= DEFAULT_MIN_DOUBLE_CHECK_SUPPORT
         or sample.grounding.doc_support_count >= 2
@@ -94,6 +338,23 @@ def _supports_minimum_double_check(sample: QuestionSample) -> bool:
 
 
 def _type_pattern_allowed(sample: QuestionSample) -> bool:
+    if sample.question_type in {"vqa"}:
+        return True
+    edges = sample.subgraph.get("edges", []) if isinstance(sample.subgraph, dict) else []
+    if not edges:
+        return True
+    node_types = _node_type_lookup(sample)
+    for edge in edges:
+        if not isinstance(edge, dict):
+            return False
+        head_type = _canonical_type_name(str(edge.get("head_type", "")))
+        tail_type = _canonical_type_name(str(edge.get("tail_type", "")))
+        if not head_type:
+            head_type = node_types.get(canonicalize_entity(str(edge.get("head", ""))), "")
+        if not tail_type:
+            tail_type = node_types.get(canonicalize_entity(str(edge.get("tail", ""))), "")
+        if not _edge_type_allowed(head_type, str(edge.get("relation", "")), tail_type):
+            return False
     return True
 
 
@@ -103,16 +364,16 @@ def _answer_unique(index: QuestionGenerationIndex, sample: QuestionSample) -> bo
     if sample.question_type == "two_hop_tail":
         first, second = sample.subgraph["edges"]
         head = canonicalize_entity(first["head"])
-        rel1 = normalize_text(first["relation"]).casefold()
-        rel2 = normalize_text(second["relation"]).casefold()
+        rel1 = _relation_key(first["relation"])
+        rel2 = _relation_key(second["relation"])
         final_tail = canonicalize_entity(second["tail"])
         intermediate_answers = set()
         for triple in index.outgoing_by_head.get(head, []):
-            if normalize_text(triple.normalized_relation).casefold() != rel1:
+            if _relation_key(triple.normalized_relation) != rel1:
                 continue
             pivot = canonicalize_entity(triple.tail)
             for downstream in index.outgoing_by_head.get(pivot, []):
-                if normalize_text(downstream.normalized_relation).casefold() != rel2:
+                if _relation_key(downstream.normalized_relation) != rel2:
                     continue
                 if canonicalize_entity(downstream.tail) == final_tail:
                     intermediate_answers.add(pivot)
@@ -190,12 +451,17 @@ def validate_sample_rule_based(
     external-corroboration step via ``_validate_corroboration``.
     """
     reasons: list[str] = []
+    _refresh_independent_grounding_counts(sample)
     if not _evidence_supported(index, sample):
         reasons.append("no_evidence_alignment")
     if not _type_pattern_allowed(sample):
         reasons.append("type_relation_mismatch")
     if not _answer_unique(index, sample):
         reasons.append("non_unique_answer")
+    if not _multi_hop_chain_supported_by_evidence(sample):
+        reasons.append("multi_hop_chain_not_independently_supported")
+    if not _claim_choice_options_valid(sample):
+        reasons.append("claim_choice_options_not_unique")
     if not _answer_not_leaked(sample):
         reasons.append("answer_leakage")
     if require_local_multi_source and not _supports_minimum_double_check(sample):
@@ -210,7 +476,8 @@ def validate_sample_rule_based(
         and "insufficient_multi_source_support" not in reasons
     )
     sample.grounding.is_fully_grounded = not reasons
-    sample.grounding.answer_supported = "no_evidence_alignment" not in reasons
+    sample.grounding.answer_supported = "no_evidence_alignment" not in reasons and "claim_choice_options_not_unique" not in reasons
+    sample.grounding.multi_hop_chain_supported = "multi_hop_chain_not_independently_supported" not in reasons
     sample.grounding.validation_mode = "rule_only"
     sample.quality.validation_status = "passed" if not reasons else "rejected"
     sample.quality.rejection_reasons = reasons
@@ -275,9 +542,16 @@ def validate_sample_model_based(
         sample.quality.essay_score = essay_score
         sample.quality.essay_rationale = essay_rationale
     else:
+        question_for_judge = sample.question
+        answer_for_judge = sample.answer.text
+        if sample.question_type == "claim_choice":
+            question_for_judge = _claim_choice_judge_question(sample)
+        elif sample.question_type == "two_hop_tail":
+            question_for_judge = _two_hop_judge_question(sample)
+            answer_for_judge = build_claim_text(sample)
         verdict = judge_claim(
-            question=sample.question,
-            answer=sample.answer.text,
+            question=question_for_judge,
+            answer=answer_for_judge,
             evidence_bundle=serialized_bundle,
             model_config=model_config,
             cache=cache,
@@ -311,18 +585,21 @@ def _validate_experiment_sample(
     sandbox verdict into validation_status / rejection_reasons / grounding
     fields.
 
-    If the sandbox was unavailable (``verdict == 'skipped'``) we preserve
-    the prior rule-only behaviour: the sample stays ``passed`` with
-    ``validation_mode = rule_only``. This keeps existing runs working until
-    SANDBOX_HOST is filled in.
+    Experiment-code samples fail closed: missing, skipped, timed-out, or
+    otherwise non-passing sandbox evaluations are rejected because their
+    algorithmic label is not verified without a runtime harness.
     """
     metadata = sample.metadata if isinstance(sample.metadata, dict) else {}
     sandbox_eval = metadata.get("sandbox_evaluation") if isinstance(metadata.get("sandbox_evaluation"), dict) else None
 
-    if not sandbox_eval or sandbox_eval.get("verdict") == "skipped":
-        sample.grounding.validation_mode = "rule_only"
-        sample.grounding.validation_status_detail = "experiment_spec_ready"
-        sample.quality.validator_version = "experiment_rule_only_v1"
+    if not sandbox_eval:
+        sample.grounding.validation_mode = "sandbox"
+        sample.grounding.validation_status_detail = "sandbox_missing"
+        sample.quality.validator_version = "experiment_sandbox_v1"
+        sample.quality.validation_status = "rejected"
+        sample.quality.rejection_reasons = ["missing_sandbox_evaluation"]
+        sample.grounding.double_checked = False
+        sample.grounding.is_fully_grounded = False
         return sample
 
     verdict = str(sandbox_eval.get("verdict", "skipped"))
